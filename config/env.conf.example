# 第三方 API 配置（使用 Claude Code 订阅的用户无需配置此文件）
# 复制为 env.conf 后填入你的信息：cp config/env.conf.example config/env.conf
# 格式：KEY=VALUE（不要加 export，不要加引号）

# API 端点
# ANTHROPIC_BASE_URL=https://api.stepfun.com/anthropic

# API Key
# ANTHROPIC_API_KEY=你的api_key

# 模型名（用服务商自己的模型名）
# ANTHROPIC_MODEL=step-3.5-flash
